#include <stdio.h>
int main()
{
    int a = 5;
    stand(time(0)) ;
    n = rand() ℅100 +1 ; // generate a random number between 1 to 100
    printf(“the number is%d”,n) ;
    while (1)
        {
            if (a==n)
            {
                break;
            }
        }
    return 0;
}
